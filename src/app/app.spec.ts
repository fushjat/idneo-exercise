import { describe, it, expect } from 'vitest';

// Skipped placeholder to avoid running Angular TestBed-based tests in Vitest.
describe.skip('App (skipped tests)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
